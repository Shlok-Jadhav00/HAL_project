# AEIA — Python Dependencies
#
# These are tested MINIMUM versions known to work well together and to have
# stable PyQt5 wheel availability. They are intentionally NOT exact `==` pins,
# because asserting exact patch versions without actually building and testing
# the environment first would be a guess dressed up as a fact — and that is
# exactly what this document set is trying to avoid.
#
# ACTION FOR WHOEVER SETS UP THE ENVIRONMENT FIRST (per NFR-012):
#   1. Create a virtual environment using Python 3.10 or 3.11 (recommended —
#      broadest, most stable PyQt5 + PyInstaller wheel compatibility at time
#      of writing; confirm before using a newer Python minor version).
#   2. pip install -r requirements.txt
#   3. Once the app runs and all test_cases.md checks pass, run:
#        pip freeze > requirements.lock.txt
#      and commit requirements.lock.txt as the real, exact-pinned build spec.
#      From then on, THAT file (not this one) is the source of truth for
#      reproducible builds.

PyQt5>=5.15
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
reportlab>=4.0
nltk>=3.8
spacy>=3.7
pyinstaller>=6.0
